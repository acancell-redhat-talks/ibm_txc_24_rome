https://catalog.redhat.com/software/container-stacks/detail/609560d9e2b160d361d24f98
podman build -t <image name> .
podman run --name <container name> --detach --publish 8080:8000 <image name>
curl -v http://127.0.0.1:8080/

podman ps
podman logs <container name>
ps -ax | grep http.server
podman exec -it <container name> ps -ax | grep http.server
ss -tupln
podman exec -it <container name> ss -tupln
podman kill <container name>; podman rm <container name>

podman run --name <container name> --detach --publish 8080:8000 --entrypoint date <image name>
podman exec -it <container name> ps -ax
podman ps -a; podman logs <container name>

podman cp second.html <container name>:/var/www/html/
curl -v http://127.0.0.1:8080/second.html
podman kill <container name>; podman rm <container name>; podman run --name <container name> --detach --publish 8080:8000 --entrypoint date <image name>
curl -v http://127.0.0.1:8080/second.html

podman login quay.io
podman image ls
podman tag localhost/<image name> quay.io/acancell-redhat-talks/ibm_txc_24_rome/demo-container:latest
podman push quay.io/acancell-redhat-talks/ibm_txc_24_rome/quay.io/acancell-redhat-talks/ibm_txc_24_rome/demo-container:latest
